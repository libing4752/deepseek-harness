In this deepseek-harness checkout, when a task develops, extends, or fixes the harness (a plugin, preset, skill, Agent Note, composition, or anything under packages/, apps/, examples/, docs/, or .agents/), load and follow the `harness-dev-mode` skill first: search for existing open-source plugins to reuse, then plan → code → test → full regression with fixes; extend only as a plugin (never edit core source in place); and finish by capturing experience into `.agents/notes/` and `.agents/skills/`.
