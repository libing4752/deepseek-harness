In this deepseek-harness checkout, commit work split by feature — never fold several independent features into one mixed commit. Each feature or area gets its own commit with a focused message. When a working tree already contains several unrelated features, split them into separate commits (mixed reset to the parent, then stage per feature) before pushing; use `--force-with-lease` when the rewrite moves an already-pushed commit.
